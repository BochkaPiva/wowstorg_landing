# Telegram Integration

Future integration boundary for internal notifications.

Planned events:

- new lead created;
- lead status changed;
- content publish requested;
- production errors that require attention.

Secrets must stay server-side only. Browser code must never receive Telegram bot tokens.
