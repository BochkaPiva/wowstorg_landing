# Lead Capture

Future feature boundary for public lead forms, validation, anti-spam, analytics attribution,
and submit flows.

Planned responsibilities:

- collect event requests from the landing page;
- validate and normalize contact data;
- attach UTM/source metadata;
- persist leads through the selected backend;
- notify the internal team through Telegram.
